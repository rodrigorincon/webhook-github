#To run this project, run:

```
bundle install

rails db:create

rails db:migrate

rails s -p 3000 -b '0.0.0.0'
```

#To run tests, run:

```
bundle install

rails db:create

rails db:migrate

rspec
```
